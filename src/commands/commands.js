// Function-File-Host für das Ribbon-Command-Manifest.
// Aktuell werden alle Buttons als ShowTaskpane-Aktionen deklariert, daher
// muss hier keine ExecuteFunction-Aktion registriert werden. Die Datei
// wird trotzdem benötigt, weil das Manifest ein FunctionFile referenziert.
Office.onReady(() => {
  // Bewusst leer.
});
